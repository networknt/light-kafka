# If there is only one streams in your application, you can define the applicationId here. Otherwise, override it.
applicationId: placeholder
bootstrapServers: localhost:9092
processingGuarantee: exactly_once
replicationFactor: 3
# Only set to true right after the streams reset.
cleanUp: false